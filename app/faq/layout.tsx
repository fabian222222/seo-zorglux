import React from "react"

export const metadata = {
  title: 'Zorglux Zorglub - Questions & Answers'
}
export default function ClientLayout({ children }: {
  children: React.ReactNode
}) {
  return children
}
